# Client-side (Vite) – used at build time
VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""

# Optional app metadata
VITE_APP_NAME="LogMyTravel"
VITE_APP_ENV="production"

# Server-side (Netlify Functions) – set only in Netlify UI
# Do NOT commit real values
SUPABASE_URL=""
SUPABASE_SERVICE_ROLE_KEY=""
SMTP_HOST=""
SMTP_PORT="465"
SMTP_USERNAME=""
SMTP_PASSWORD=""
SMTP_FROM=""
